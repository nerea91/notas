El archivo de DNS esta en /etc/resolv.conf para poner dns hay que escribir nameserver dns ej: nameserver 8.8.8.8

Los archivos de red estan en /etc/sysconfig/network/routes

Para añadir la puerta de enlace route add default gw ip